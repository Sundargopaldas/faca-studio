/**
 * Índice dos Algoritmos
 * Exporta todos os algoritmos disponíveis
 */

const LayoutOptimizer = require('./layoutOptimizer');
const KnifeGenerator = require('./knifeGenerator');

module.exports = {
  LayoutOptimizer,
  KnifeGenerator
};

